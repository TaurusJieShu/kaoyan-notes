# [61. 旋转链表](https://leetcode.cn/problems/rotate-list/)

```c
//  
// Created by shuji on 2026/3/24.  
//  
/**  
 * Definition for singly-linked list. * struct ListNode { *     int val; *     struct ListNode *next; * }; */
struct ListNode *rotateRight(struct ListNode *head, int k) {  
    // 排除0个，1个元素，k不合法的情况  
    if (head == NULL || head->next == NULL || k <= 0) {  
        return head;  
    }  
  
    // 格式化 k 值  
    struct ListNode *current = head;  
    int cnt = 0;  
    while (current != NULL) {  
        cnt++;  
        current = current->next;  
    }  
    k = k % cnt;  
    if (k == 0) {  
        return head;  
    }
  
    struct ListNode *prev = NULL;  
    current = head;  
    struct ListNode *prev_fast = NULL;  
    struct ListNode *fast = head;  
  
  
    for (int i = 0; i < k; i++) {  
        fast = fast->next;  
    }  
  
    while (fast != NULL) {  
        prev_fast = fast;  
        fast = fast->next;  
        prev = current;  
        current = current->next;  
    }  
  
    prev->next = NULL;  
    prev_fast->next = head;  
    return current;  
}
```
# [876. 链表的中间结点](https://leetcode.cn/problems/middle-of-the-linked-list/)
```c
/**  
* Definition for singly-linked list.  
 * struct ListNode { *     int val; *     struct ListNode *next; * }; */
   
struct ListNode* middleNode(struct ListNode* head) {  
    struct ListNode* slow = head;  
    struct ListNode* fast = head;  
  
    while(fast!=NULL && fast->next!=NULL){  
        slow = slow -> next;  
        fast = fast -> next;  
        fast = fast -> next;  
    }  
  
    return slow;  
}
```

# [141. 环形链表](https://leetcode.cn/problems/linked-list-cycle/)

```c
/**  
* Definition for singly-linked list.  
 * struct ListNode { *     int val; *     struct ListNode *next; * }; */
bool hasCycle(struct ListNode *head) {  
    struct ListNode *fast = head;  
    struct ListNode *slow = head;  
  
    while (fast != NULL && fast -> next != NULL){  
        fast = fast->next;  
        if(fast == slow) return true;  
        fast = fast->next;  
        if(fast == slow) return true;  
  
        slow = slow->next;  
    }  
    return false;  
}
```

# [189. 轮转数组](https://leetcode.cn/problems/rotate-array/)

```c
//  
// Created by shuji on 2026/3/24.  
//  
void rotate(int *nums, int numsSize, int k) {  
    k = k % numsSize;  
    if (k == 0) return;  
    int a[k];  
    int index = 0;  
    for (int i = 0; i < k; i++) {  
        a[i] = nums[numsSize - k + i];  
    }  
    for (int i = numsSize - k - 1; i >= 0; i--) {  
        nums[i + k] = nums[i];  
    }  
    for (int i = 0; i < k; i++) {  
        nums[i] = a[i];  
    }  
}
```
> 
> 这里其实很有意思的一点是，我们通常希望：“**数据一次就到我们需要的位置**”，👆的思想就是这样。
> 但其实有个很重要的思想就是，如果我们**不用一次完成**，先近一点，在近一点，再近一点，或许就能完成，而👇的思想就是非一次性完成，而且代码更有可读性。
> 这类题不一定要盯着：
> - 这个数最后去哪
> -那个数最后去哪
> 更重要的是看：
> - 整体结构能不能拆成几段
> - 这些段能不能通过某种标准操作重组
> - 有没有一种“中间状态”更容易实现
> 

```c
void reverse_array(int *nums, int numsSize) {  
    int temp = 0;  
    for (int i = 0; i < numsSize / 2; i++) {  
        temp = nums[i];  
        nums[i] = nums[numsSize - 1 - i];  
        nums[numsSize - 1 - i] = temp;  
    }  
}  
  
void rotate(int *nums, int numsSize, int k) {  
    k = k % numsSize;  
    if (numsSize < 2 || k == 0) return;  
    reverse_array(nums, numsSize);  
    reverse_array(nums, k);  
    reverse_array(nums + k, numsSize - k);  
}
```

## 第一问：
**这题是在算值，还是在改结构？**
如果是改结构，就要警惕它可能不是普通模拟题。
## 第二问：
**目标结果能不能拆成几段来看？**
比如：
- 前半段/后半段
- 左区间/右区间
- 奇数位/偶数位
## 第三问：
**有没有标准操作可以重复利用？**
比如：
- reverse
- swap
- 双指针
- 局部旋转