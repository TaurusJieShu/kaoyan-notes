![[images/Pasted image 20260315151945.png]]
1、首先看到题型是“数列和”求极限
2、这里区分的主要是在分母，如果分母的适度放大会导致分母极限形式改变那就需要去用定积分定义去做或许会更快
- 夹逼准则：适用于适度放大缩小后极限不变的情况【但如果不一样能证明上下有界】
- 定积分定义：准确求出极限，先提可爱因子1/n，然后整理为i/n的式子，化成积分式，求极限
![[images/Pasted image 20260315152639.png]]
![[images/Pasted image 20260315152958.png]]
这里强调 **分母中的（分子可再求和极限）**，如果变化部分$例1: 1,2,3,...,n。例2: 1^2,2^2,3^2,...,n^2$
- 分母变化部分相比主体部分$n^2$是“同量级”：用定积分定义
- 分母变化部分相比主体部分$n^2$是“次量级”：用夹逼准则


![[images/Pasted image 20260315162945.png]]
这题里重点是：
1、分母的变化量是不变量的次量级 -> 夹逼准则
2、合并之后得到极限趋于同一个极限求极限时有 1/n 可爱因子，并用定积分定义来收敛最终的极限

![[images/Pasted image 20260315171105.png]]
>而如上题，一般来说我们要去对比，左边和右边的差别，A-B中，一般的我们把余式，向主要的式子靠拢，达到合并的目的，如果不方便和做不出来，那么再考虑把主要的式子拆开做

![[images/Pasted image 20260315180100.png]]
![[images/Pasted image 20260315175958.png]]
上述两个式子总结：
1、先尝试缩小放大，用夹逼，如果不方便，尝试用对数
2、用对数的时候，连乘可以变连加（n! 阶乘也可以变连加）

### 递推关系定义的数列极限
差值法：$(x_{n+1}-x_n \ge 0)（或 (\le 0)） ⇒ 数列 ({x_n}) 单调递增（或单调递减）。$
比值法：$若 ({x_n}) 为正项数列且 (\frac{x_{n+1}}{x_n} \ge 1)（或 (\le 1)） ⇒ 数列单调递增（或单调递减）。$
函数递推结论：
1、$若 (x_{n+1}=f(x_n)) 且 (f(x)) 单调递增：$
$则 (x_1 \le x_2 \Rightarrow {x_n}) 单调递增，(x_1 \ge x_2 \Rightarrow {x_n}) 单调递减。$
2、$若 (x_{n+1}=f(x_n)) 且 (f(x)) 单调递减，则可知<奇偶子列>分别单调，且：$
- $奇列 (x_1 \le x_3 \Rightarrow {x_n}) 单调递增，(x_1 \ge x_3 \Rightarrow {x_n}) 单调递减。$
- $偶列 (x_2 \le x_4 \Rightarrow {x_n}) 单调递增，(x_2 \ge x_4 \Rightarrow {x_n}) 单调递减。$
（即奇偶列分别看单调性，如果要“求极限”的话，则可以看奇偶列的极限是否相等）

![[images/Pasted image 20260315205537.png]]
【注意】在做递推关系的数列极限时，需要注意常用的不等式
- 在$\sqrt{AB}$时，有$2AB \leq A^2+B^2 \to\sqrt{AB}\leq\frac12(A+B)$ 这里通常A+B的形式很特殊，需要注意
![[images/Pasted image 20260316013236.png]]
本题要点：如果只用证明存在
- 用所有的不等式，不需要完全趋于一个值，而是固定在一个区间，因单调则必收敛
- f(x_0)这种类型是有限量，因为通常都是适度放大，所以最大可以放大到无穷，但这里因为是arctanx的函数，所以一定是收敛的
- 证明唯一实根 -> 证明单调性，也就是建立函数，求导，判定单调，得结论

# AI总结
### 1. **夹逼准则的判定与使用**

#### 适用场景：
- 当分母的变化部分相比主体部分为**次量级**时使用。
- 适用于**适度放大或缩小后极限不变**的情况。

#### 使用步骤：
- **步骤1**：识别数列或函数的变化部分是否为“次量级”。
- **步骤2**：通过**放大或缩小**，找到该数列或函数的上下界。
- **步骤3**：如果上下界极限**相同**，则原数列或函数的极限也等于该值。
- **步骤4**：若无法直接放大或缩小，可以尝试使用**对数**将连乘转化为连加，便于处理。

#### 注意事项：
- **适用条件**：适用于分母的变化部分不是“同量级”时，即其影响相对于主体部分较小。
- **优点**：能够**证明上下有界**，从而求得极限。

---

### 2. **定积分定义的判定与使用**

#### 适用场景：
- 当分母的变化部分相比主体部分为**同量级**时使用。
- 适用于**分母变化部分与主体部分同量级**，且能够**转化为定积分形式**的情况。

#### 使用步骤：
- **步骤1**：识别分母的变化部分是否与主体部分为“同量级”。
- **步骤2**：提出**可爱因子** $\frac{1}{n}$，并将其转化为 $\frac{i}{n}$ 的形式。
- **步骤3**：将数列和式转化为定积分形式，通常为：
  $$
  \lim_{n \to \infty} \sum_{i=1}^n f\left(\frac{i}{n}\right) \cdot \frac{1}{n} = \int_0^1 f(x) \, dx
  $$
- **步骤4**：计算定积分，从而得到数列极限的值。

#### 注意事项：
- **适用条件**：适用于分母变化部分与主体部分“同量级”时。
- **优点**：能够**准确求解极限**，尤其适用于数列和式的极限计算。

---

### 3. **两者之间的区别与选择建议**

| 特征             | 夹逼准则               | 定积分定义                  |
| -------------- | ------------------ | ---------------------- |
| 分母变化部分与主体部分的关系 | “次量级”              | “同量级”                  |
| 适用场景           | 无法准确计算极限，但可证明上下界一致 | 可转化为定积分形式              |
| 优点             | 能够证明极限存在           | 能够准确计算极限               |
| 主要使用方法         | 放缩法                | 提出 $\frac{1}{n}$，转化为积分 |

---

### 4. **应用实例总结**

- **例1**：分母变化部分为 $1,2,3,\ldots,n$，主体部分为 $n^2$，变化部分是“次量级”，使用**夹逼准则**。
- **例2**：分母变化部分为 $1^2,2^2,3^2,\ldots,n^2$，主体部分为 $n^2$，变化部分是“同量级”，使用**定积分定义**。

---

### 5. **技巧补充**

- 如果无法直接使用夹逼准则，尝试使用**对数**，将连乘转换为连加，如：
  $$
  \ln\left(\prod_{i=1}^n a_i\right) = \sum_{i=1}^n \ln a_i
  $$
  从而简化计算。

---

以上即为夹逼准则与定积分定义的判定与使用准则，可根据题目中数列或函数的变化部分与主体部分的量级关系选择合适的工具进行求解。


