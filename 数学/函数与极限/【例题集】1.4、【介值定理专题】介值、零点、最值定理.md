### 1. 📌 **基本概念**【注意：三个定理都建立「有限闭区间」内「连续」】

- **介值定理**：【连续两值】 $已知f(a),f(b)的值\Rightarrow f(a),f(b)之间的任意值都能取到$
  - **条件**：$f(x)$ 在 $[a, b]$ 上连续，且 $f(a) \neq f(b)$。  
  - **结论**：存在 $c \in (a, b)$，使得 $f(c) = k$（$k$ 在 $f(a)$ 与 $f(b)$ 之间）。  
  - **应用**：**证明函数在区间内可取任意中间值。**

- **零点定理**：【连续异号】 $f(a)\cdot f(b)<0 \Rightarrow \exists \xi, f(\xi)=0$ 下一定有零点
  - **条件**：$f(x)$ 在 $[a, b]$ 上连续，且 $f(a) \cdot f(b) < 0$。  
  - **结论**：存在 $c \in (a, b)$，使得 $f(c) = 0$。  
  - **应用**：**证明方程在区间内有解。**

- **最值定理**：【连续极值】$已知连续闭区间\Rightarrow f(x)在闭区间一定有最大最小值$
  - **条件**：$f(x)$ 在 $[a, b]$ 上连续。  
  - **结论**：$f(x)$ 在 $[a, b]$ 上有最大值和最小值。  
  - **应用**：**求解函数在区间上的最值。**

---

### 2. 📈 **定理对比表**

| 定理名称     | 条件                                          | 结论                                                        | 应用场景                   |
| -------- | ----- | ----- | ----- |
| **介值定理** | $f(x)$ 在 $[a, b]$ 上连续，$f(a) \neq f(b)$      | 存在 $c \in (a, b)$ 使得 $f(c) = k$（$k$ 在 $f(a)$ 与 $f(b)$ 之间） | 证明函数在区间内取到特定值          |
| **零点定理** | $f(x)$ 在 $[a, b]$ 上连续，$f(a) \cdot f(b) < 0$ | 存在 $c \in (a, b)$ 使得 $f(c) = 0$                           | 证明方程 $f(x) = 0$ 在区间内有解 |
| **最值定理** | $f(x)$ 在 $[a, b]$ 上连续                       | $f(x)$ 在 $[a, b]$ 上取得最大值和最小值                              | 寻找函数在区间内的最值            |

### 3. 🎯 **题型要点**
![[images/Pasted image 20260318232544.png]]
1、这题出现了 $\xi$ 但属于开区间内非负连续，但他公式里$x_1,x_2,...x_n$都是有限值，如果没有闭区间条件，这里就能创造闭区间条件：
- 取$x_{min}=\min{x_1,x_2,...x_n}$ $x_{max}=\max{x_1,x_2,...x_n}$ 就知道有 $m=f(x_{min})<f(\xi)<f(x_{max})=M$ 这样的结论
2、由介值定义可知，$\xi ∈ [x_{min},x_{max}]$，使得式子$f(\xi)=\sqrt[n]{f(x_1)f(x_2)...f(x_n)}$ 成立 

![[images/Pasted image 20260318232558.png]]
例2：【构造辅助函数】$f(\xi+l)=f(\xi)\Rightarrow g(x)=f(x+l)-f(x)$【用零点定理】$证明g(x)有零点$
【构造辅助函数用零点定理】
1、令$F(x)=f(x+l)-f(x)$ $x ∈ [0,1-l]$ (这里要保证$x+l$不超过$f(x)$的范围)
2、$\because F(0)=f(l)-f(0)\ge0$ $F(1-l)=f(1)-f(1-l)\le0$ 
3、$\therefore ∃\xi ∈ [0,1-l]，使 F(\xi)=0$  
![[images/Pasted image 20260318232610.png]]
例3：$f(\xi+\frac14)=f(\xi)\Rightarrow g(x)=f(x+\frac14)-f(x)，证明g(x)有零点$ 👉 **零点定理**
![[images/Pasted image 20260319012408.png]]
这里考虑逆否命题反证法更方便，也就是让我证明构造函数等于0存在不方便，就可以假设它不存在，那么要么大于0要么小于0，那么假设大于0的时候有什么结论，就能得到f(x+1/4)>f(x)推导过去两个点就不可能相等，所以矛盾，所以可以证明
![[images/Pasted image 20260319012521.png]]


![[images/Pasted image 20260318232630.png]]
例4：$g(x)=f(x)+x$  👉 利用极限判断符号变化  👉 **零点定理**
![[images/Pasted image 20260319012945.png]]

### 🎯 **题型要点归纳**

> 
> 📌 **核心思路**：根据题型特征，选择合适的定理，构造合适的辅助函数，判断连续性与区间性质。
> 
> 📌 **题型锚点**：**“连续 + 存在 ξ + 等式/极值”**
> 

- **构造函数**：  
  - 若函数在某点平移后相等，如 $f(\xi + l) = f(\xi)$，构造 $g(x) = f(x + l) - f(x)$。  
  - 若等式复杂（如乘积），可取对数 $\ln▪️$，转化为加法。

- **存在性问题&证明存在解**：  
  - 关键点：**连续 + 存在 $\xi$ + 等式/极值**。  
  - 常见构造方法：如 $g(x) = f(x + l) - f(x)$。
  - 使用**零点定理**或**介值定理**，构造辅助函数 $g(x)$，证明其有零点。

- **极值问题**：  
  - 利用**最值定理**，结合函数连续性，在闭区间上寻找最大值或最小值。







